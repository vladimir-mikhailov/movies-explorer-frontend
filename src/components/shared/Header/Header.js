import { useEffect } from 'react';
import './Header.css';
import Logo from '../Logo/Logo';

const Header = () => {
  useEffect(() => {

  }, []);

  return (
    <header className='header'>
      <Logo />
    </header>
  );
};

export default Header;
